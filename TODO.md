# SpreadWord 実装TODO

## データベース関連

### 1. スキーマの更新 ✅
- [x] `user_words`中間テーブルの作成
- [x] リレーションの設定
- [x] マイグレーションの実行

### 2. データアクセス層の実装
- [ ] `user_words`テーブルのCRUD操作関数の作成
  ```typescript
  // 例: ユーザーの単語進捗を更新する関数
  async function updateWordProgress(userId: string, wordId: number, complete: boolean) {
    // データベース更新処理
  }
  ```
- [ ] ユーザーごとの単語一覧取得関数
  ```typescript
  // 例: ユーザーの単語リストを取得する関数
  async function getUserWords(userId: string) {
    // ユーザーの単語一覧を取得
  }
  ```

### 3. APIエンドポイントの作成
- [ ] 単語進捗更新API
  - エンドポイント: `POST /api/words/progress`
  - リクエストボディ:
    ```typescript
    {
      wordId: number;
      complete: boolean;
    }
    ```
- [ ] ユーザー単語一覧取得API
  - エンドポイント: `GET /api/words/user`
  - レスポンス:
    ```typescript
    {
      words: {
        id: number;
        word: string;
        complete: boolean;
        // その他の単語情報
      }[];
    }
    ```

### 4. フロントエンド実装
- [ ] 単語一覧ページの更新
  - 完了状態の表示
  - 完了/未完了の切り替えUI
- [ ] 進捗管理機能
  - 完了した単語のカウント
  - 進捗バーの表示

## 注意点
1. 中間テーブル (`user_words`) の役割
   - ユーザーと単語の関係を管理
   - 各ユーザーの単語ごとの完了状態を保持

2. データの整合性
   - ユーザーが削除された時は、関連する`user_words`レコードも削除（CASCADE）
   - 単語が削除された時も同様

3. パフォーマンス考慮
   - 必要に応じてインデックスを追加
   - N+1問題を避けるためJOINを活用

4. エラーハンドリング
   - 存在しないユーザーや単語へのアクセス
   - 重複データの防止

## 新機能の実装

### 1. ダッシュボード機能
- [ ] スキーマの更新
  - [ ] 学習履歴テーブルの追加（学習日時、正解/不正解など）
- [ ] データアクセス層の実装
  - [ ] 学習統計情報取得関数の作成
  - [ ] 最近の学習履歴取得関数の作成
- [ ] APIエンドポイントの作成
  - [ ] 学習統計情報取得API
  - [ ] 学習履歴取得API
- [ ] フロントエンド実装
  - [ ] ダッシュボードページのレイアウト
  - [ ] 進捗グラフの実装
  - [ ] 学習履歴一覧の表示
  - [ ] 統計情報の可視化

### 2. 4択クイズ機能
- [ ] スキーマの更新
  - [ ] クイズ結果テーブルの追加
- [ ] データアクセス層の実装
  - [ ] ランダム単語取得関数の作成
  - [ ] クイズ結果保存関数の作成
- [ ] APIエンドポイントの作成
  - [ ] クイズ問題生成API
  - [ ] クイズ結果保存API
- [ ] フロントエンド実装
  - [ ] クイズページのUI実装
  - [ ] 問題表示と選択肢の実装
  - [ ] 即時フィードバックの実装
  - [ ] 結果表示の実装
